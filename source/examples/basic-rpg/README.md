# Basic RPG Example

A minimal Edwin RPG demonstrating the core features: locations, navigation, items, and an NPC.

## Running

From the `source/` directory:

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal and navigate to `examples/basic-rpg/index.html`.
